{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "shokunin/SHIP_RECORD.schema.json",
  "title": "Shokunin Ship Record",
  "description": "FROZEN CONTRACT (locked decision D4, 2026-07-01). The pipeline's final artifact and the ONLY interface consumed by deploy adapters (Dokploy or client-side). Any change requires an ADR on both sides.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "release", "sha", "pipelineUrl", "gates", "images", "shippedAt"],
  "properties": {
    "schemaVersion": { "const": "1.0" },
    "release": {
      "type": "string",
      "pattern": "^R_\\d{3}$",
      "description": "Matches .shokunin/release/RELEASE_{NNN}.md"
    },
    "sha": { "type": "string", "pattern": "^[0-9a-f]{7,40}$" },
    "pipelineUrl": { "type": "string", "format": "uri", "description": "The forge run URL - the pasted proof for reports (VERIFIED != DECLARED)." },
    "gates": {
      "type": "object",
      "additionalProperties": false,
      "required": ["structure", "lint", "typecheck", "test"],
      "properties": {
        "structure": { "enum": ["pass"] },
        "lint": { "enum": ["pass"] },
        "typecheck": { "enum": ["pass"] },
        "test": { "enum": ["pass"] },
        "coverage": { "type": "number", "minimum": 0, "maximum": 100 },
        "e2e": { "enum": ["pass", "nightly", "off"] }
      },
      "description": "A SHIP_RECORD only exists if blocking gates passed - failed values have no representation by design."
    },
    "images": {
      "type": "array",
      "description": "Empty array allowed when gates.package=false (profile S: build artifact shipped instead).",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["service", "image", "digest", "version"],
        "properties": {
          "service": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
          "image": { "type": "string", "minLength": 1, "description": "Full pullable ref registry/service:version" },
          "digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
          "version": { "type": "string", "minLength": 1, "description": "Changesets version from the service package.json" }
        }
      }
    },
    "shippedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Consumed by post-release-metrics to compute the J+7 / J+30 checkpoints."
    }
  }
}
